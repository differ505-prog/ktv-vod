---
description: 每次優化或 debug 完成後的自動化規範 (push commit + 部署到 NAS)
globs:
alwaysApply: true
---

# Push Commit + 部署到 NAS（不可違反的規則）

> **Single Source of Truth**: `CONSTITUTION.md` §5.1
> **使用者已明示**: 「每次優化或 debug 完，push commit 跟部署到 nas，這動作要納入系統憲法，不用我每次提醒」

## 何時觸發

任何對程式碼、組態、腳本的**優化**或**debug 修正**完成後，自動執行下列兩件事。**不需使用者提醒**。

## 必做兩件事（順序不可顛倒）

### 1. Git Commit + Push
```bash
git add -A
git commit -m "<描述這次改動>"
git push origin <branch>
```

### 2. 部署到 NAS

依改動範圍選擇：

| 改動範圍 | 部署方式 |
|---|---|
| 只改 `public/`、`server.js`、`pipeline_server.js` | rsync 到 NAS（見下） |
| 改 `ktv-pipeline/` | `bash ktv-pipeline/deploy_via_ssh.sh ktv-pipeline` |
| 改 `server.js` | rsync + SSH 重啟 `ktv-brain` container |
| 部署失敗 | `bash ktv-pipeline/rollback.sh` |

**rsync 指令**（NAS = `192.168.31.47`, port 22, user `vibe`, 密碼 `05050505`）:
```bash
SSHPASS='05050505' rsync -avz \
  -e 'ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -p 22' \
  --checksum \
  --exclude='.DS_Store' \
  public/ \
  vibe@192.168.31.47:/home/vibe/ktv-vod/public/
```

純前端改動 rsync 後**不需要重啟容器**，user 重整瀏覽器即生效。

## 例外（可跳過部署）

- 純粹本地測試 / 探索性改動
- 改的是 `*.md` 文件
- user 明確說「先不要部署」

## 如何驗證部署成功

```bash
ssh -o StrictHostKeyChecking=no vibe@192.168.31.47 \
  "docker ps --format '{{.Names}}: {{.Status}}' | grep ktv"
```
或直接 `curl http://192.168.31.47:3001/mobile.html` 確認 200。