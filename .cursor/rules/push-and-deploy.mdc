---
description: push + 部署流程 + 對話效率規則 (避免繞彎與 token 浪費)
globs:
alwaysApply: true
---

# Push + 部署 + 對話效率規則

> **Single Source of Truth**: `CONSTITUTION.md` §5
> **使用者明示**:
>   1. 「push commit 跟部署到 nas，這動作要納入系統憲法，不用我每次提醒」
>   2. 「檢視本對話框有無繞彎行為、過度囉唆浪費 token 行為」

---

## A. 每次優化 / debug 完成必做 (依 §5.1)

1. **git commit + push**
2. **部署到 NAS**（依改動範圍）：

| 改動範圍 | 部署方式 |
|---|---|
| 只改 `public/` | rsync 到 NAS（volume mount 已啟用，容器直接讀到） |
| 改 `server.js` | rsync + docker cp + 重啟 container |
| 改 `pipeline_server.js` | rsync 到 NAS |
| 改 `ktv-pipeline/` | `bash ktv-pipeline/deploy_via_ssh.sh ktv-pipeline` |
| 部署失敗 | `bash ktv-pipeline/rollback.sh` |

**server.js 部署流程**（因 container 內沒有 volume mount /app 目錄）：
```bash
SSHPASS='05050505'
rsync -avz \
  -e 'ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -p 22' \
  --checksum --exclude='.DS_Store' \
  server.js vibe@192.168.31.47:/home/vibe/ktv-vod/
ssh "${SSH_OPTS[@]}" "$NAS_HOST" \
  "docker cp /home/vibe/ktv-vod/server.js ktv-brain:/app/server.js && docker restart ktv-brain"
```

**public/ 部署流程**（`./public:/app/public:ro` 已啟用，rsync 到 host 容器直接讀到）：
```bash
SSHPASS='05050505' rsync -avz \
  -e 'ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -p 22' \
  --checksum --exclude='.DS_Store' \
  public/ vibe@192.168.31.47:/home/vibe/ktv-vod/public/
```

**例外**: 純本地測試 / 改 `*.md` / user 明示「先不要部署」

**KTV 測試 URL**: http://192.168.31.47:3001/mobile.html

---

## B. 對話效率規則 (依 §5.4)

### b1. 不要把專業需求誤判為 meta 議題
user 訊息短、含術語時 → 當成「對系統的需求」處理。
**不要**立刻用評分框架、不要預設成「對 agent 行為的建議」。

### b2. 不主動為「可能問題」設計保險
只在 user **明確問**「怎麼辦」「如何處理」「怕⋯」時才設計保險邏輯。

### b3. 小於 5 步的工作不開 TodoWrite
直接做。TodoWrite 留給多檔案、多步驟、需追蹤狀態的場景。

### b4. AskQuestion 後直接執行
user 已在 AskQuestion 表達意圖並選項 → 直接做。
**不要**再用 TodoWrite 重述同樣的事。

### b5. 「自動化」明示後自動執行，不反問
當 user 說「⋯自動⋯」「⋯不用提醒」「⋯納入系統⋯」→ **直接做**。
不要問「要不要我幫你 push？」「要不要部署？」。

### b6. 寫完規則/設定後停下來等確認
新增憲法、cursor rule、hook 後，**只總結改了什麼檔案**，
**不要立刻示範執行**。

### b7. 完成報告的長度上限
- 改 1~3 檔: 一行「完成」+ 改動清單。
- 改 4+ 檔或含架構變更: 才用「總結」section，**不超過 5 行**。
- **不開**「✅ 完成」「🎉 上線」「下次起自動生效」慶祝式標題。

### b8. token 密度
- 表格用於資料對照 (NAS 設定、評分結果)，**不用於「做了什麼」**。
- 一行能說完的事不要用 bullet 列表。
- 程式碼引用優先用 code reference (`startLine:endLine:path`)，不貼整段。

### b9. 不要在同一個對話中重複示範同一個新規則
新規則寫完就停下來。**不要**「既然寫了規則，順便示範一次執行」。

### b10. 部署前截圖確認視覺效果
改 `public/` UI 元件後，**先截圖確認按鈕/元件在瀏覽器裡真的可見，再部署**。
視覺確認比 logic review 更快速發現問題。browser snapshot 和截圖同時用：
- `browser_take_screenshot` — 視覺最終確認
- `browser_snapshot` — 結構快速確認（refs 可用來點擊）